name: week4-lab3-sonarqube

services:
  backend:
    build:
      context: ./backend
    ports:
      - '8000:8000'

  frontend:
    build:
      context: ./frontend
    depends_on:
      - backend
    ports:
      - '4173:80'
